"use client"

import { useLayoutEffect } from "react"

export const useParallaxEffect = (): void => {
	useLayoutEffect(() => {
		// GSAP parallax effect disabled - would require GSAP installation
		// This is a placeholder function for future implementation
		console.log("Parallax effect placeholder - GSAP not installed")
	}, [])
}
