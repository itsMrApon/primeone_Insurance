"use client"

import { useLayoutEffect } from "react"

export default function useTextAnimation3() {
    useLayoutEffect(() => {
        // GSAP text animation disabled - would require GSAP installation
        // This is a placeholder function for future implementation
        console.log("Text animation 3 placeholder - GSAP not installed")
    }, [])
}
